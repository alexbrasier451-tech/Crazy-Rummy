# Stage 1.1.2 baseline reconciliation status

**Controller mandate:** The project owner requested implementation of Stages
1.1.2 through 1.1.7 on 30 July 2026. This is recorded as the production-opening
decision for the current controlling task.

## 1. Baseline revision

The inspected beta is the owner-approved `1.0.0` workspace on branch `master`.
It has no Git commits, so no immutable commit SHA, deployment identifier, or
signed deployment URL exists. The pre-change supported `pnpm check` passed in
full on 30 July 2026. Stage 1.1.1 concept captures remain compatible as design
references, but their `UNCOMMITTED` label remains truthful.

## 2. Contract inventory

Stable contracts are the existing routes, engine rules/schema, player-scoped
projection, host authority, staged/pending/accepted/rejected/uncertain action
feedback, recovery expiry, saved summaries, PWA honesty, DOM reading order,
keyboard/non-drag paths, and the existing test selectors. Presentation
composition and registered local assets are the only planned mutation surface.

## 3. Delta log

| Change | Source | Impact | Disposition | Owner / verification |
| --- | --- | --- | --- | --- |
| Owner opens Stages 1.1.2–1.1.7 | Controlling Codex task | Production presentation programme | Compatible with approved Concept B | Controller / complete QA rerun |
| Legacy splash is excluded | Stage 1.1.1 asset decision | Startup art and PWA bytes | Required visual and budget rework | Controller / build and PWA |
| Semantic v1.1 roles and assets added | Stage 1.1.2 | All route programmes | Compatible; no behaviour contract change | Controller / unit, visual, full suite |

## 4. Conflict ownership

Rules, privacy, routes, authority, recovery, persistence, and accepted-event
semantics remain unchanged. The controller owns visual composition, tokens,
assets, post-acceptance motion, performance, and integration. Any discovered
semantic conflict stops the affected visual treatment and falls back to the
existing contract.

## 5. Integration window

The window begins from the locally verified `1.0.0` workspace and ends with a
fresh full-suite run, route/state visual review, asset-budget report, and final
source fingerprint. Protected areas are `src/engine`, `src/online`, privacy
allowlists, route paths, persisted schemas, and PWA update truth. A change to
those contracts aborts and reopens reconciliation.

## 6. Rerun requirements

Every presentation package runs its focused route tests. Shared integration
runs unit, build, lobby, transport, online game, game flow, PWA, GitHub Pages,
Stage 7 results, local game, responsive captures, forced colours, and reduced
motion. Final evidence must continue to state that a Git commit SHA and
physical-device review are absent until supplied.

## Gate disposition

The owner instruction opens implementation, but it cannot manufacture the
missing immutable Git baseline. Therefore code work may be verified in this
workspace, while a production-release claim remains blocked until the source
is committed/fingerprinted and the external Stage 1.1.7 evidence is supplied.
